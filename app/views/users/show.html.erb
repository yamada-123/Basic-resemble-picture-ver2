<h1><%= @user.name %>のページ</h1>

<p>メールアドレス:<%= @user.email %></p>

<p>プロフィール画像:<%= image_tag(@user.image.url) if @user.image.url.present? %></p>

<%= link_to "投稿一覧画面にもどる", pictures_path %>
<br>
<%= link_to "新規投稿画面に移る", new_picture_path %>
<br>
<%= link_to "プロフィールを編集する", edit_user_path(@user.id) %>
<br>
<%= link_to "お気に入りを表示する", favorites_path %>